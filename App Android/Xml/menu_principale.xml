<!-- Layout principale del menu iniziale -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutPrincipale"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="20dp"
    android:layout_marginTop="20dp"
    tools:context="progettotesi.menu_principale">

    <!-- Pulsante apertura cronologia -->
    <Button
        android:id="@+id/btnCronologia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cronologia"
        app:layout_constraintBottom_toTopOf="@+id/btnMappa"
        app:layout_constraintEnd_toStartOf="@+id/btnCancellaCronologia"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.49" />

    <!-- Pulsante cancellazione cronologia -->
    <Button
        android:id="@+id/btnCancellaCronologia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancella \nCronologia"
        app:layout_constraintBottom_toTopOf="@+id/btnMappa"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.979"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Pulsante apertura mappa -->
    <Button
        android:id="@+id/btnMappa"
        android:layout_width="130dp"
        android:layout_height="113dp"
        android:layout_marginBottom="88dp"
        android:text="Seleziona località\nprevisioni"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Pulsante apertura previsione ML -->
    <Button
        android:id="@+id/btnPrevisioneAI"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="377dp"
        android:text="Previsione modello ML"
        app:layout_constraintBottom_toTopOf="@+id/btnMappa"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>